<p id="notice"><%= notice %></p>
<%= render_title "Thesis:  "+@thesis.name,"" %>

<div class="container">
  <div class="span5 offset2 well">
  <% @activities.each do |activity| %>
        <%= link_to activity.name,thesis_activity_path(activity), :class => "btn btn-primary btn-block" %><br>
  <% end %>
  </div>
</div>

